/**
 * Types Supabase générés depuis le schéma.
 * Exécuter "npm run gen:types" pour régénérer (supabase login + .env.local requis).
 * En attendant, réexport des types minimaux.
 */
export type { Database, Json } from './types'
